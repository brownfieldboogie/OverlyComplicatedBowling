{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",
    "ConnectionStrings": {
        "PostgreSQL": "Host=postgres; Database=bowling; Username=user; Password=pw;"
        //"PostgreSQL": "Host=localhost; Port=15432; Database=bowling; Username=user; Password=pw;"
    }
}
